




@media (min-width: 768px) {
    .carousel-inner {
        width: calc(50% - 10px); /* 2 slides por fila en tablet */
        margin-bottom: 20px;
    }
}

@media (min-width: 1024px) {
    .container_img{
        width: 289px;
        height: 316px;
    }
    .carousel-inner {
        width: calc(25% - 10px); /* 4 slides por fila en tablet */
        margin: 0 18px;
    }

    .carousel-control-prev {
        left: 0.5;
    }
    
    .carousel-control-next {
        right: 0.5;
    }

    .btn_film{
        width: 136px;
        height: 40px;
        font-size: 14px;
    }
}

//cuando hago click en cada punto
//saber la posicion de ese punto
//aplicar un transform translateX al grande
//quitar la clase activo de todos los puntos
//Añadir la clase activo al punto que se hizo click
function initializeCarousel() {
//     const itemCarousel = document.querySelector('.carousel-inner');
//     const point = document.querySelectorAll('.point');


//     //recorrer todos los puntos
//     point.forEach((eachPoint, i) => {
//         //asignamos un click a cada punto
//         point[i].addEventListener('click', () => {
//             //guardar la posicion del punto
//             let position = i;
//             //calculando la posicion
//             //cuando la posicion es 0 transform X es 0
//             //cuando la posicion es 1 transformX es -50%
//             let operation = position * -50;
//             //movemos
//             itemCarousel.style.transform = `translateX(${operation}%)`;

//             //recorremos los puntos
//             point.forEach((eachPoint, i) => {
//                 //quitamos la clase activo
//                 point[i].classList.remove('active')
//             })
//             //añadir la clase activo en el punto donde se hizo click
//             point[i].classList.add('active')
//         });
//     });
// }